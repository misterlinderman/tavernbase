NODE_ENV=development

# Tavern Base uses split env files — copy and configure:
#   server/.env.example → server/.env
#   client/.env.example → client/.env
#   config/establishment.example.json → config/establishment.json
